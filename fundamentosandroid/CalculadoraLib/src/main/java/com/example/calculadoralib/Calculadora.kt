package com.example.calculadoralib

class Calculadora {
    companion object{
        fun soma(a:Int, b:Int) = a+b
        fun subtrai(a: Int,b: Int) = a - b
        fun multiplica(a: Int,b: Int) = a * b
        fun divide(a: Int, b: Int)= a/b
    }
}